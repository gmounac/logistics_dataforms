* Added .db to .gitignore to remove the database in the repository.
* Slightly generalized the user_from_session() in src/auth.py so it accepts both HTTP requests and WebSockets:
* Make _append() use the complete container timeline
* Added more tests for cross-stuffing
* Stop generic event edits from creating impossible records
* Added CLI
